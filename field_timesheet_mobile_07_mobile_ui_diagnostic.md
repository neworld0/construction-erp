# FIELD 출역부 모바일 UI 진단

## Before

- 근로자, 역할, 인원수/공수, 시간, 단가 유형, 메모가 하나의 넓은 테이블 열에 배치됐다.
- 360px 화면에서는 모든 입력 열을 동시에 보려면 가로 스크롤이 필요할 가능성이 높았다.
- 근로자 검색은 있었지만 각 입력값의 라벨과 우선순위가 모바일에서 충분히 명확하지 않았다.

## After

- 근로자별 독립 카드에 보이는 라벨을 제공한다.
- 모바일에서는 카드가 한 열, 공수/시간은 두 열의 짧은 숫자 입력으로 배치된다.
- 근로자 검색 입력은 전폭이고 선택 결과는 안전한 요약으로 표시된다.
- 하단 저장/제출 영역은 모바일에서 sticky이며 44px 이상 터치 대상이다.
- 실제 브라우저 연결은 이 실행 환경에서 사용할 수 없어, 렌더링 검증은 HTTP 응답·구조 테스트와 CSS 검토로 수행했다.
