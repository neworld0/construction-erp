# UI Decision

데스크톱에서도 읽기 쉬운 카드형 출역 행을 사용하고, 640px 이하에서는 한 열 카드와 하단 sticky 저장 영역으로 전환한다.

각 카드의 입력 우선순위는 근로자, 노무역할, 공수, 시간, 단가유형, 메모다. 근로자 검색은 기존 AJAX API와 숨김 `worker_id`를 그대로 사용한다. 역할 자동 반영과 WorkerMaster FK 저장 계약은 변경하지 않는다.
