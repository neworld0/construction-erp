# FIELD 출역부 근로자 검색 Root Cause

기존 출역부는 활성 `WorkerMaster` 전체를 일반 선택 상자에 표시하는 방식이었다. 근로자 수가 늘면 이름과 직종으로 빠르게 찾을 수 없고, 선택 실수도 확인하기 어렵다.

프로젝트에는 이미 FIELD용 JSON/fetch 사용 패턴이 있으나 검색 선택 라이브러리는 없다. 따라서 새 의존성 없이 FIELD 전용 검색 API와 화면 내 결과 목록을 사용한다.

검색 대상은 활성 근로자의 이름, 기본 노무 역할명, 기본 노무 역할 코드다. 주민등록번호, 연락처, 계좌번호, 주소는 검색과 응답 모두에서 제외한다.
