# FIELD 출역부 근로자 검색 UI 결정

`/app/field/labor/timesheets/new/`의 각 출역 행에서 이름 또는 직종을 입력하면 최대 20개의 안전한 후보가 표시된다. 후보를 누르면 숨김 `worker_id` 입력값과 기본 노무 역할이 채워진다.

검색 API는 FIELD 역할과 요청 프로젝트의 접근 권한을 먼저 확인한다. 현재 모델에는 근로자-프로젝트 배정 관계가 없으므로, 권한이 있는 현장 사용자는 회사의 활성 근로자를 검색할 수 있다. 근로자별 현장 배정 제한은 별도 데이터 모델과 운영 정책이 준비된 뒤 P2 후속 과제로 다룬다.

JavaScript가 비활성인 경우에도 안전한 이름/기본 역할만 가진 선택 상자를 제공한다.
