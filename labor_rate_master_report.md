# LABOR-RATE-MASTER-01 운영 보고서

## 적용 결과

기존 `LaborRateTable`과 HQ 단가표 UI를 재사용했다. `seed_labor_rates`는 2026-08-01부터 적용되는 회사 공통 일단가를 등록한다.

| 역할 | 일단가 |
|---|---:|
| 보통인부 | 160,000 |
| 포장공 | 210,000 |
| 장비공 | 250,000 |
| 다짐공 | 220,000 |
| 도색공 | 200,000 |

기존 활성 역할 코드가 LAB 코드와 달라도 동일한 한국어 역할명이라면 같은 기본 단가를 등록한다. 따라서 WorkerMaster의 역할 FK를 수정하지 않는다.

## 제출 정책

제출 시 모든 출역 행의 단가를 먼저 해결한다. 하나라도 누락되면 초안을 유지하고 다음처럼 안내한다.

`직종 단가 등록이 필요합니다. 장비공 단가가 등록되지 않았습니다. HQ에서 노무 역할 단가를 먼저 등록해 주세요.`

모든 단가가 있으면 `TimesheetLine.unit_rate`와 `amount`를 저장한 뒤 제출한다. 0원 단가는 승인 상태로 넘어가지 않는다.

## 실제 로컬 확인

`seed_labor_rates`를 실제 실행했고, 역할·단가 스냅샷에서 기존 출역부 3번의 김로컬/박포장 행이 각각 160,000원/210,000원으로 `RATE_RESOLVED`임을 확인했다.

## 남은 운영 경계

현재 월간 급여 생성은 `LaborWorkLedger`를 집계하며, 제출된 TimesheetLine을 원장으로 자동 승격하지는 않는다. 이번 작업은 출역부의 단가 스냅샷과 제출 차단을 고친 범위다. 출역부→노무원장 승격 정책은 별도 P1 설계가 필요하다.
