# LOCAL-OPS-RESET-HQ-RISK-01 Report

- Current database: Project 0, OPEN CRITICAL/HIGH RiskFinding 1, valid operational project risks 0.
- `RiskFinding #1` is a demo seed row and is no longer dashboard-visible.
- Shared risk visibility is used by HQ operating hub, HQ risk list, CEO main summary, and CEO project risk rollups.
- Local reset dry-run identifies the row as `DEMO_SEED_RISK` with `DELETE_LOCAL_RESET`; AuditLog and RiskRule remain preserved.
- No reset apply or direct DB deletion was performed in this task.
