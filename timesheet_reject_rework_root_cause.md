# Root Cause

서비스 수정 가드는 이미 `REJECTED`를 허용했지만 FIELD 화면은 반려 상태를 별도로 해석하지 않았습니다. 따라서 반려 사유·재제출 안내·재제출 버튼이 없었고, 화면의 비고 저장은 서비스 가드보다 먼저 실행될 여지도 있었습니다.

`get_timesheet_workflow_state`를 단일 판정 기준으로 두고, 수정 전에 라인 저장 서비스의 가드를 통과하게 변경했습니다.
