제목: construction-erp 스테이징 검증을 위한 Linux 서버·DB 복구 권한·Nginx/Gunicorn 설정 정보 요청

안녕하세요.

construction-erp ERP 시스템의 제한적 스테이징 파일럿 전 검증을 진행하려고 합니다.

현재 로컬 애플리케이션 검증은 통과했으나, 실제 Linux staging host 정보와 isolated restore DB 권한이 제공되지 않아 RELEASE-1-STAGING-SMOKE-RERUN이 HOLD 상태입니다.

이번 요청은 production 배포가 아니라, 제한적 staging pilot 가능 여부를 판단하기 위한 사전 검증입니다.

아래 정보를 제공해 주시면 실제 Linux staging host에서 Gunicorn, Nginx, HTTPS health check, DB restore drill, RBAC smoke를 재실행하겠습니다.

---

## 1. 요청 목적

목적:
- construction-erp 실제 Linux staging 환경 검증
- Gunicorn/Nginx 구동 확인
- HTTPS 또는 staging endpoint `/healthz/` 확인
- CEO/HQ/FIELD/LABPAY 주요 화면 smoke 확인
- FIELD 권한 차단 등 RBAC 확인
- isolated restore DB에 백업 복구 드릴 수행
- 제한적 staging pilot 가능 여부 판정

이번 작업에서 하지 않는 것:
- production 배포 안 함
- production DB 접속/쓰기 안 함
- active staging DB에 restore 안 함
- 실제 전자카드/개인정보 파일 업로드 안 함
- secret 값 출력/공유 안 함
- migration 생성 안 함
- destructive cleanup 안 함

---

## 2. 필요한 서버 정보

아래 항목을 채워 주세요.

1. Linux staging host identifier 또는 endpoint:
   - 예: staging-erp.internal 또는 masked IP

2. 접속 방식:
   - SSH 가능 여부:
   - 접속 계정:
   - 접속 가능 IP 또는 VPN 조건:
   - sudo 가능 여부:

3. 프로젝트 경로:
   - 예: /srv/construction-erp

4. Python virtualenv 경로:
   - 예: /srv/construction-erp/.venv

5. 배포 branch / commit:
   - branch:
   - commit SHA:
   - 참고: LABPAY 목록 성능 패치는 commit `8613d5b`에 포함되어 있어야 합니다.

---

## 3. Django 환경 설정 정보

secret 값은 공유하지 말고, 존재 여부와 경로만 알려 주세요.

1. settings module:
   - 예: config.settings.prod 또는 config.settings.staging

2. secure env file path:
   - 예: /etc/construction-erp/staging.env

3. 필수 환경변수 상태:
   - SECRET_KEY: PRESENT / MISSING
   - DEBUG: false 여부
   - ALLOWED_HOSTS: staging domain 포함 여부
   - CSRF_TRUSTED_ORIGINS: staging origin 포함 여부
   - DATABASE_URL 또는 DB_* 설정: PRESENT / MISSING
   - STATIC_ROOT:
   - MEDIA_ROOT:
   - SESSION_COOKIE_SECURE:
   - CSRF_COOKIE_SECURE:
   - SECURE_SSL_REDIRECT:

주의:
- SECRET_KEY, DB password, token, private key 값은 절대 메시지에 적지 말아 주세요.
- 값이 아니라 PRESENT/MISSING 또는 파일 경로만 필요합니다.

---

## 4. PostgreSQL / Restore Drill 정보

isolated restore drill을 위해 아래 중 하나가 필요합니다.

A안: DB 담당자가 미리 빈 restore DB 생성
- restore DB name:
- DB host:
- DB user:
- 권한 범위:
- active DB와 분리되어 있다는 증빙:

B안: staging operator에게 CREATEDB 권한 제공
- CREATEDB 가능 여부:
- 허용 DB name pattern:
  - 예: construction_erp_restore_YYYYMMDD
- cleanup/drop 권한 여부:

필수 조건:
- restore DB 이름에 restore, drill, test, demo 중 하나가 포함되어야 합니다.
- production DB나 active staging DB에는 restore하지 않습니다.
- 복구 후 Project count, AuditLog count, migration 상태만 확인합니다.
- raw 개인정보는 출력하지 않습니다.

백업 파일 정보:
- backup archive path:
- expected SHA256 checksum:
- pg_restore 사용 가능 여부:
- backup retention owner:

---

## 5. Gunicorn 정보

아래 항목을 알려 주세요.

1. Gunicorn systemd service name:
   - 예: construction-erp-gunicorn

2. service file path:
   - 예: /etc/systemd/system/construction-erp-gunicorn.service

3. bind address:
   - 예: 127.0.0.1:8000

4. worker count:
5. timeout:
6. access log path:
7. error log path:

허용이 필요한 명령:
- systemctl status <service>
- journalctl -u <service> -n 100
- 필요 시 systemctl restart <service>

---

## 6. Nginx 정보

아래 항목을 알려 주세요.

1. Nginx site config path:
   - 예: /etc/nginx/sites-available/construction-erp-staging

2. enabled config path:
   - 예: /etc/nginx/sites-enabled/construction-erp-staging

3. staging domain:
4. HTTPS 적용 여부:
5. proxy_pass 대상:
6. static alias 경로:
7. media alias 또는 protected media 정책:
8. client_max_body_size:
9. proxy_read_timeout:
10. access/error log path:

허용이 필요한 명령:
- nginx -t
- systemctl status nginx
- 필요 시 systemctl reload nginx

---

## 7. Static / Media / Upload 정책

아래 정책을 확인해 주세요.

1. STATIC_ROOT 경로:
2. collectstatic 실행 여부:
3. Nginx static serving 허용 여부:
4. MEDIA_ROOT 경로:
5. media 파일 직접 공개 여부:
6. Excel upload/download 파일 보관 정책:
7. media backup 방식:
   - local archive / S3 / EBS snapshot / 기타
8. media encryption at rest 여부:
9. retention 기간:
10. 한글 파일명 처리 정책:

주의:
- media에 실제 전자카드, 엑셀, 증빙 파일이 포함될 수 있으므로 공개 경로 설정은 신중히 확인해야 합니다.

---

## 8. Staging 테스트 계정 요청

실제 개인정보가 없는 테스트 계정만 제공해 주세요.

1. CEO 테스트 계정:
   - username:
   - password 전달 방식:

2. HQ 테스트 계정:
   - username:
   - password 전달 방식:

3. FIELD 테스트 계정:
   - username:
   - password 전달 방식:
   - 배정 프로젝트:
     - 가능한 경우 `OPS1B-RERUN-SAMPLE-001`

4. admin/staff 계정 필요 여부:
   - username:
   - password 전달 방식:

주의:
- 비밀번호는 이 메시지에 직접 쓰지 말고 별도 안전한 방식으로 전달해 주세요.

---

## 9. 실행 승인 범위

아래 작업을 staging에서 수행해도 되는지 승인 부탁드립니다.

허용 요청:
- git branch/commit 확인
- python manage.py check
- python manage.py makemigrations --check --dry-run
- WSGI import 확인
- Gunicorn status 확인
- 필요한 경우 Gunicorn restart
- nginx -t
- 필요한 경우 nginx reload
- HTTPS `/healthz/` 확인
- login / CEO / HQ / FIELD / LABPAY route smoke
- FIELD 권한 차단 확인
- isolated restore DB에 pg_restore 실행
- restore DB에서 Project count, AuditLog count, migration state 확인
- source scan 및 secret/PII 노출 여부 확인

금지:
- production host 접속
- production DB 접속/쓰기
- active staging DB restore
- 실제 전자카드 파일 업로드
- migration 생성
- destructive cleanup
- secret 출력
- 권한/RBAC/CSRF/보안 설정 완화
- production release 승인

---

## 10. 승인자 정보

아래 승인자를 지정해 주세요.

1. Release Owner:
2. Infra Owner:
3. DB Owner:
4. Security/Media Policy Owner:
5. limited staging pilot 최종 승인자:

승인 문구 예시:
“위 범위 내에서 construction-erp RELEASE-1-STAGING-SMOKE-RERUN을 non-production staging 환경에서 수행하는 것을 승인합니다. production release는 본 승인 범위에서 제외합니다.”

---

## 11. 현재 상태 참고

현재까지 확인된 상태:
- Local smoke: PASS
- LABPAY 목록 성능 패치: commit `8613d5b`
- manage.py check: PASS
- migration drift: 없음
- local regression: 64 passed
- source scan: PASS
- secret/raw PII 기록 없음
- 실제 Linux staging host 검증: 아직 미수행
- isolated restore drill: 권한 부족으로 미수행

위 정보를 채워 주시면 다음 단계인 `RELEASE-1-STAGING-SMOKE-RERUN`을 실제 staging host에서 재실행하겠습니다.

감사합니다.